import pumpItFaxtInterface from "./pumpItFaxtInterface";
import frax from "./frax";
import token from "./token";

const contractDefinitions = { frax, pumpItFaxtInterface, token } as const;

export default contractDefinitions;
